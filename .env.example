# Discord Bot Token — from Discord Developer Portal > Bot > Token
DISCORD_BOT_TOKEN=

# How long to wait for Discord's own link preview before the bot posts one.
# Go duration string (e.g. 3s, 1500ms). Defaults to 3s. 0 replies immediately.
PREVIEW_GRACE_PERIOD=3s
