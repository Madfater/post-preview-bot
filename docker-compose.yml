services:
  bot:
    image: ghcr.io/madfater/post-preview-bot:latest
    pull_policy: always
    restart: unless-stopped
    environment:
      - DISCORD_BOT_TOKEN=${DISCORD_BOT_TOKEN}
      - PREVIEW_GRACE_PERIOD=${PREVIEW_GRACE_PERIOD:-3s}
